Dependency Injection
----------------------
Dependency Injection (DI) in Spring is a design pattern where an external container manages and provides
the required dependencies of a class, instead of the class creating them internally.

It is a concrete implementation of Inversion of Control (IoC), meaning the control of object creation and
lifecycle management is shifted from the developer's manual code to the Spring Framework.

The Core Problem and the DI Solution
---------------------------------------
In standard Java, if a Car class needs an Engine, we typically use the new keyword inside the Car class:

public class Car{
    private Engine engine = new Engine(); //Tightly coupled
}

This causes tight coupling; if the Engine constructor changes, the Car class breaks.

With Spring DI, the Car class simply declares that it needs an Engine, and the Spring IoC Container automatically
supplies (injects) that Engine instance at runtime:

@Component
public class Car{

    private final Engine engine;

    //Spring automatically injects the Engine bean here
    public Car(Engine engine){
        this.engine = engine;
    }
}

Note: IoC is the broader principle where control of object creation and management is transferred to the Spring Container.
      Dependency Injection is the mechanism through which Spring provides the required dependencies to objects.

3 Main Types of Dependency Injection
-------------------------------------

Spring supports three primary variants to inject dependencies into a class:

1. Constructor Injection (Recommended)

    Dependencies are provided through the class constructor.
    This is the industry-standard choice because it ensures required dependencies are never null and allows fields
    to be marked as final (promoting immutability).

Example 1:

@Service
public class EmployeeService{

    private final EmployeeRepository empRepo;

    //@Autowired annotation is optional is the class has only one constructor
    public EmployeeService(EmployeeRepository eRepo){
        this.empRepo = eRepo;
    }

}

Example 2:

@Component
public class OrderService{

    private final PaymentService;

    //@Autowired annotation is optional is the class has only one constructor
    public OrderService(PaymentService ps){
        this.PaymentService = ps;
    }
}

2. Setter Injection

    Dependencies are supplied through public setter methods.
    This approach is ideal for optional or modifiable dependencies that can be changed or assigned later.

Example:

@Component
public class OrderService{

    private PaymentProcessor paymentProcessor;

    @Autowired
    public void setPaymentProcessor(PaymentProcessor paymentProcessor){
        this.paymentProcessor = paymentProcessor;
    }
}

3. Field Injection

    Dependencies are injected directly into the fields using reflection by placing the @Autowired annotation right on the variable.
    While convenient and brief, it is generally discouraged because it hides dependencies and makes unit testing
    outside of a Spring container difficult.

Example:

@Component
public class EmployeeService{

    @Autowired
    private EmployeeRepository employeeRepository;
}

The difference between Dependency Injection and IoC container
--------------------------------------------------------------

IoC is the broader principle where control of object creation and management is transferred to the Spring Container.
Dependency Injection is the mechanism through which Spring provides the required dependencies to objects.

Note: Constructor injection is generally preferred because dependencies are explicit,
      required dependencies can be made final, and the class is easier to test.

      @Autowired tells Spring where a dependency needs to be injected.
      OR It tells Spring to inject a suitable dependency into a constructor, field, or setter.

@Component,@Controller, @RestController, @Service, @Repository
----------------------------------------------------------------

These are all ways of telling Spring:
"This class should be managed as a Spring bean."

@Component: A generic Spring-managed component
@Controller: Used for Spring MVC controllers
@RestController: Commonly used for REST APIs
@Service: Typically used for business logic
@Repository: Used for data-access classes

What happens when a spring-boot application starts?
--------------------------------------------------
When the Spring Boot application starts, SpringApplication.run() creates and initializes the
Spring ApplicationContext.

Through component scanning, Spring discovers classes annotated with components such as @Service, @Repository, and @Controller
and registers them as beans.

Spring then creates and configures those beans and resolves their dependencies through Dependency Injection.

Auto-configuration configures the required Spring infrastructure based on the application's dependencies.
Once the context is initialized, the application is ready to handle requests

@SpringBootApplication
        |
Spring Boot starts
        |
ApplicationContext
        |
Component Scanning
        |
Find @Service / @Repository / @Controller
        |
Register Beans
        |
Create Beans
        |
Resolve Dependencies
        |
Dependency Injection
        |
Application Ready

The difference between @Controller and @RestController
-------------------------------------------------------

@Controller and @RestController are used to handle HTTP requests in a Spring application.
The main difference is what we want to return to the client.
@Controller is mainly used when we want to return a web page or view, like an HTML page.
For example, if a user requests /employees, the controller can return an employees.html page.
On the other hand, @RestController is mainly used for building REST APIs, where we want to return data, usually in JSON format,
instead of a web page. For example, if we call /employees, it can return a list of employees as JSON.
@RestController is basically a combination of @Controller and @ResponseBody. @ResponseBody tells Spring to put the returned Java object
directly into the HTTP response, and Spring converts it into JSON. So, in a Spring Boot microservices application, we generally use
@RestController because our services communicate through REST APIs and exchange JSON data.

The easiest way to remember it is: @Controller -> return a page, @RestController -> return data/JSON.

Why do we use @RestController?: We use @RestController to create REST APIs that handle HTTP requests and return data, usually JSON,
                                directly in the HTTP response. It is convenient because it combines @Controller and @ResponseBody.

REST API Picture
-----------------

GET     /employees              -> Get all employees
GET     /employees/101          -> Get employee 101
POST    /employees              -> Create employee
PUT     /employees/101          -> Update employee 101
DELETE  /employees/101          -> Delete employee 101

HTTP Method       Purpose

GET               Read
POST              Create
PUT               Update
DELETE            Delete

1. @RequestMapping - A general purpose mapping annotation which is used at the class level
                  It avoids repeating the common URL.

Example: Without it
         @GetMapping("/employees")
         @PostMapping("/employees")
         @DeleteMapping("/employees/{id}")

         With it
         @RestController
         @RequestMapping("/employees")
         public class EmployeeController{
            @GetMapping
            @PostMapping
            @DeleteMapping("/{id}")
         }

         Much cleaner code

2. @GetMapping - Used when we want to handle an HTTP GET request

                 @GeMapping - maps an HTTP GET request to a controller method. It is a shortcut for
                 @RequestMapping(method = RequestMethod.GET)

Example:
        @GetMapping("/employees")
        public List<Employee> getEmployees(){
            return employeeService.getAllEmployees();
        }

3. @PostMapping

   Used for creating a resource.

   @PostMapping
   public Employee createEmployee(@RequestBody Employee employee) {
       return employeeService.createEmployee(employee);
   }

   The client might send: JSON

   {
       "name": "Sneha",
       "department": "IT",
       "salary": 70000
   }

   Request:

   POST /employees

4. @RequestBody - tells Spring that "take the data from the HTTP request body and convert into this Java object"

    Example:

        @PostMapping
        public void creatEmployee(@RequestBody Employee employee){
            employeeService.creatEmployee(employee);
        }

        Client sends: JSON

        {
            "name": "Sneha",
            "department": "IT"
        }

        Spring converts it roughly like:

        JSON
         | to
        Employee object

        So inside your method:
        employee.getName()
        gives:
        Sneha

Note: RequestBody = data coming inside the request body.

      Usually used with:
        POST
        PUT
        PATCH

5. @PathVariable - Used to bind values from HTTP request URL to the Argument in the method signature

Example:

    URL: /employees/101

    @GetMapping("/{id}")
    public Employee getEmployeeWithId(@PathVariable Long id){
        return employeeService.getEmployeeWithId(id);
    }

    Here Spring extracts id - 101 and gives it to Long id. So now id = 101

6. @RequestParam - extracts a query parameter, such as /employees?department=IT

Example:  GET /employees?department=IT
          here
          department=IT is the query parameter

          @GetMapping
          public List<Employee> getEmployeesByDept(@RequestParam String department){
            return employeeService.getAllEmployeesWithDept(department);
          }

          Request: GET /employees?department=IT

          Spring gives: department="IT"

Difference between the @PathVariable and @RequestMapping
---------------------------------------------------------

    @PathVariable

    Value is part of the URL path:

    /employees/101
    @PathVariable Long id

    Used when identifying a specific resource.

    @RequestParam

    Value comes after ?

    /employees?department=IT
    @RequestParam String department

    Used commonly for filtering, searching, sorting, pagination, etc.

7. @PutMapping - Used for Updating a Resource

Example:
        Request - PUT /employees/101

        JSON - {
                    "name": "Sneha",
                    "dept": "ME",
                    "salary": 80000
                }

          @PutMapping
          public Employee updateEmployeeById(@PathVariable Long id, @RequestBody Employee employee){
                return employeeService.updateEmployeeById(id,employee);
          }

We are using two annotations together here:
          @PathVariable
          @RequestBody

          Because: 101 -> @PathVariable and Employee JSON -> @RequestBody

8. @DeleteMapping - Used to delete a resource

Example: Request: DELETE /employees/101

        @DeleteMapping
        public void deleteEmpById(@PathVariable Long id){
            employeeService.deleteEmpById(id);
        }

        Spring extracts: 101
        using @PathVariable

One important thing about @RequestMapping
-------------------------------------------

We can actually use @RequestMapping instead of a specialized annotations

For example:

@RequestMapping(
    value="{/id}",
    method=RequestMethod.GET
)
public void getAllEmployeeById(@PathVariable int id){
...
}

This is equivalent to:

@GetMapping("/{id}")
public void getEmpById(@PathVariable int id){
...
}

Similarly,

@RequestMapping(
    value = {"/id"}
    method = RequestMethod.PUT
)
public void updateEmpById(@PathVariable int id, @RequestBody Employee employee){
...
}

This is equivalent to:

@PutMapping("/{id}")
public void updateEmployeeById(@PathVariable int id, @RequestBody Employee employee){
...
}

So:

@GetMapping, @PostMapping, @PutMapping, and @DeleteMapping are specialized shortcuts for @RequestMapping.

Important Notes:
------------------

- @RequestMapping is used to map HTTP requests to controller classes or methods. It can specify the URL path,
  HTTP method, headers, parameters, and other request conditions.

- @RequestMapping is a general-purpose mapping annotation that can handle different HTTP methods, whereas
  @GetMapping specifically maps GET requests. @GetMapping is a composed shortcut for
  @RequestMapping(method = RequestMethod.GET)

- @PathVariable extracts a value from the URL path, such as /employees/101,
  whereas @RequestParam extracts a query parameter, such as /employees/search?department=IT

  @PathVariable is used when we want to identify specific data
  @RequestParam is used in operations like searching, sorting, filtering, pagination etc.

- @RequestBody tells Spring to read the HTTP request body and deserialize it into a Java object,
  typically from JSON

- We can use @PathVariable and @RequestBody together. Example: Update emp with id 101, JSON passed from body

              Employee API

GET /employees
    |
 Get all employees
    |
   @GetMapping


GET /employees/101
    |
  @PathVariable
    |
Get employee with id 101
    |
   @GetMapping("/{id}")


GET /employees/search?dept=IT
    |
   @RequestParam
    |
Search employees with dept IT
    |
   @GetMapping


POST /employees
    |
   @RequestBody
    |
Create new employee
    |
   @PostMapping


PUT /employees/101
    |
  @PathVariable + @RequestBody
    |
Update employee with id 101
    |
   @PutMapping


DELETE /employees/101
    |
   @PathVariable
    |
Delete emp with id 101
    |
   @DeleteMapping

-------------------------------------
Next important concepts!!!

@ResponseBody vs @RequestBody
@Controller vs @RestController
HTTP status codes
ResponseEntity
Complete request flow: Postman -> Controller -> Service -> Repository -> DB

1. @RequestBody vs @ResponseBody
-----------------------------------

The easiest way:

@RequestBody :

HTTP request
     to
Java object

@ResponseBody :

Java object
     to
HTTP response

So they work in opposite directions.

In @RequestBody

Client sends:

{
    "name": "Sneha",
    "department": "IT",
    "salary": 70000
}

Spring converts this JSON to:

JSON
 |
Employee Java object

That's:

@RequestBody Employee employee

In @ResponseBody

Out controller returns Java Object:
Employee employee

Spring converts:

Employee Java object
 |
JSON

That's @ResponseBody.

Note: We don't need to write @ResponseBody manually when using @RestController
      Because @RestController is the combination of
      @Controller + @ResponseBody

@Controller vs @RestController
-------------------------------

@Controller is generally used for Spring MVC applications where controller methods can return views/HTML page,
whereas @RestController is designed for REST APIs and combines @Controller with @ResponseBody, so the
return value is written directly to the HTTP response body typically in JSON form.

HTTP Status Code
----------------

Whenever the server responds to a client request it not only returns the data but also other important information
like HTTP Status Codes and HTTP Status etc

HTTP Status Code helps understand whether the request is success or was a failed request.

Listing Important HTTP Status Code below:

1. 200 - OK

    Request Succeeded
    Common for - GET, PUT operations

2. 201 - CREATED

    A new resource was successfully created
    Common for - POST operation

3. 204 - NO CONTENT

   Request is succeeded but there is no response body
   Common for - DELETE

4. 400 - BAD REQUEST

    Client sent invalid data

    Example: {
        "name": "",
        "salary":-500
    }

5. 401 - UNAUTHORIZED

    The client hasn't provided valid authenticated credentials.

6. 403 - FORBIDDEN

    The client is authenticated but isn't allowed to perform the operation.

7. 404 - NOT FOUND

    Resource doesn't exist.

8. 500 - INTERNAL SERVER ERROR

    Something unexpected went wrong on the server.

    For example, an unhandled exception

ResponseEntity
---------------

In Spring Boot, ResponseEntity is a class provided by the Spring Framework that represents the entire
HTTP response. By using it as the return type in out @RestController methods, we gain full control over
configuring the HTTP status code, response headers, and response body.

The Core ComponentsResponseEntity<T> uses Java generics, where T defines the data type of the response body.
It wraps three vital parts of an HTTP response:

1. Status Code: Lets us explicitly set HTTP statuses like 200 OK, 201 Created, 404 Not Found, or 500 Internal Server Error
                instead of relying on defaults.
2. Headers: Allows us to pass essential metadata like Content-Type, Cache-Control, or our own custom security
            and tracking tokens.
3. Body: Contains the actual payload being returned to the client, such as a custom Java object, a list,
         a string, or an error details object.


The difference between @ExceptionHandler and @ControllerAdvice
---------------------------------------------------------------

1.  @ExceptionHandler - Handles a specific exception.

    Example: @ExceptionHandler(EmployeeNotFoundException.class)

    @ControllerAdvice - Makes exception handling global across controllers.

Handling unexpected exceptions in spring boot
----------------------------------------------
Suppose the database suddenly fails.

We might get:

NullPointerException
SQLException
DataAccessException

We don't want our API returning ugly internal details.

We have generic exception handler - @ControllerAdvice & @ExceptionHandler
We can use these to handle the unexpected exceptions.

Note: We generally use @ControllerAdvice for global exception handling and @ExceptionHandler methods to handle
        specific exceptions. For example, if an employee isn't found, the service can throw a custom
        EmployeeNotFoundException. The global exception handler catches it and returns a meaningful error response
        with HTTP 404. This keeps exception-handling logic separate from the controllers and provides a consistent
        error response across the application.

Handling invalid data from client in Spring Boot
--------------------------------------------------

For example let's say the client sends invalid data:

    {
        "id":109,
        "name":
        "dept":"AI",
        "salary":-5000
    }

Our application should handle this & should throw meaningful errors.
Instead of allowing bad data into our application, we want 400 - BAD REQUEST

We can implement this by using validation annotations like @NotBlank , @Valid, etc directly into our Employee entity classes

1. @NotBlank - validates a String and ensures that it is not null, empty, or composed only of whitespace.

    It rejects - "" , " " , null

2.@Positive - ensures that a numeric value is strictly greater than zero

3. @Valid - Tells spring to perform validations on our input client data coming from operations like POST, PUT

If the validation fails the controller throws MethodArgumentNotValidException before the request reaches to service and repository

@Valid
Tells Spring to validate the incoming object against its validation constraints.

@NotBlank
Ensures a String is not null, empty, or whitespace-only.

@Positive
Ensures a numeric value is greater than zero.

@NotBlank
@NotEmpty
@Negative
@Size
@Min
@Max
@Email

@RestControllerAdvice
Provides global exception handling for REST controllers and writes handler results to the response body.

@ExceptionHandler
Defines how a particular exception should be handled.

Note: How to validate request data in Spring Boot?
        We can use Jakarta Bean Validation annotations such as @NotBlank, @Positive, @Size, etc. on the request model,
        and use @Valid on the @RequestBody parameter in the controller. If validation fails, Spring throws
        MethodArgumentNotValidException. I handle this globally using @RestControllerAdvice and @ExceptionHandler and
        return a meaningful 400 Bad Request response containing the validation errors.

DTO - Data Transfer Object in Spring Boot
------------------------------------------

A DTO is simply a Java class used to transfer data between different layers of an application,
especially between the client and our API.

We don't want our database structure to automatically become our API response structure.
That's one of the major reasons we use DTOs.

Why use DTOs?
--------------
1. Separation

DTO separates API models from database entities.

2. Security

You can prevent sensitive Entity fields from being exposed.

3. Control

You decide exactly what the API accepts and returns.

4. Validation

You can put request-specific validation on request DTOs.

5. Flexibility

Database structure can change without necessarily changing our API contract.

"DTO stands for Data Transfer Object. In a Spring Boot REST API, we use request and response DTOs to separate the
API contract from the database Entity. For example, in our Employee SB application EmployeeRequest contains the
fields the client is allowed to send and can contain validation annotations, while EmployeeResponse contains the
fields we want to expose to the client. This improves separation of concerns, prevents exposing sensitive entity
fields, and allows the database model and API model to evolve independently."

Spring Boot
------------
Spring Boot makes it easier to build and run Spring applications by reducing configuration and providing
features like autoconfiguration, embedded servers, actuators, and a setup to build and run our spring application.

AutoConfiguration
-----------------

Auto Configuration is a Spring Boot feature that automatically configures the application based on the dependencies
available in the classpath and the configuration provided by the developer.

It reduces the need for manual configuration. For example, when we add spring-boot-starter-web, Spring Boot
automatically configures components required for a web application, including the embedded server and Spring MVC
infrastructure.

Spring Boot Starters / Starter Dependencies
--------------------------------------------

A Starter is a predefined collection of dependencies required for a particular functionality.
Instead of adding 10 dependencies manually, Spring Boot gives you one starter.

For example:

spring-boot-starter-web : This brings dependencies required for building web applications.

Without Starter we might need to manually add:

Spring MVC
Jackson
Tomcat
Validation
etc.

With
spring-boot-starter-web : Spring Boot brings the commonly required dependencies together.

Common starters:

1. spring-boot-starter-web

Used for: REST APIs
          Spring MVC
          Embedded Tomcat
          JSON

2. spring-boot-starter-data-jpa

Used for: JPA
          Hibernate
          Database access

3. spring-boot-starter-test

Used for: JUnit
          Mockito
          Spring Test

4. spring-boot-starter-security

Authentication
Authorization
Security filters

application.properties
-----------------------

application.properties is a configuration file used by Spring Boot to externalize application configuration
such as server port, database configuration, logging settings and custom application properties.
This allows us to change configuration without modifying the application code.

We have dependencies and automatic configuration. But applications still need configuration values.

For example:
server.port=8081

Where do we put it?
Usually:

src/main/resources/application.properties

Example

server.port=8081
spring.application.name=employee-service

Now our application runs on:
8081
instead of the default:
8080

Database example
We could configure:

spring.datasource.url=jdbc:mysql://localhost:3306/employees
spring.datasource.username=root
spring.datasource.password=password

Instead of hardcoding these inside Java.
We put it in: application.properties

This gives us:
Externalized configuration
Easier environment changes
Cleaner code
No need to modify Java code for configuration changes

application.yml file (YAML file)
--------------------------------

application.yml and application.properties solve the same basic problem.

They are two different formats for configuration.

properties -

server.port=8081
spring.application.name=employee-service

spring.datasource.url = jdbc:mysql://localhost:3306/employeeDB
spring.datasource.username = root
spring.datasource.password = root
spring.jpa.hibernate.ddl-auto=update

yml -

server:
    port: 8081
spring:
    application:
        name: employee-service

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/employeeDB
        username: root
        password: root

    jpa:
        hibernate:
            ddl-auto: update

YAML is often easier to read for complex configuration.

Note: We can have both application.properties and application.yml file in our spring boot.
      Both are supported configuration formats, but typically a project follows one format consistently.

Profiles
---------

Profiles allow us to maintain environment-specific configuration. For example, we can have separate configurations
for DEV, UAT and PROD while keeping the application code the same. We activate the appropriate profile during deployment.

Imagine our application has three environments:

DEV
UAT
PROD

Our database will be different in each environment.

DEV:
dev-db

UAT:
uat-db

PROD:
prod-db

We don't want to modify Java code every time we deploy.
That's exactly why Profiles exist.

Example
We can create:

application.properties
application-dev.properties
application-uat.properties
application-prod.properties

application.properties has Common configuration:

spring.application.name=employee-service

application-dev.properties
server.port=8081
spring.datasource.url=jdbc:mysql://dev-db:3306/employees

application-uat.properties
server.port=8082
spring.datasource.url=jdbc:mysql://uat-db:3306/employees

application-prod.properties
server.port=8080
spring.datasource.url=jdbc:mysql://prod-db:3306/employees

Now we activate the required profile.

For example:
spring.profiles.active=de

Spring Boot loads:
application.properties
+
application-dev.properties

Same application code.
Different configuration.
That's the beauty of profiles.

Ways to activate a profile
---------------------------

1 - through application.properties file - spring.profiles.active = dev
2 - thorough command line - java -jar employee.jar --spring.profiles.active=prod

In real deployments, the profile is often supplied externally rather than hardcoded.